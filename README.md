# ngang-app-backend

Runs on ~~Deno~~ **Rust**

* needs built version of **ngang-app** to function

**Settings configured in `Rocket.toml`:**
```
address = "localhost"
port = 5000
keep_alive = 5
log = "normal"
```
