# ngang-app-backend

Runs on Deno

* needs built version of **ngang-app** to function

**config.ts should be of format:**
```
const Config = {
  port: <port>,
  key: <key>
}

export default Config
```
